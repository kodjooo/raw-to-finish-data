ПЛАН РЕАЛИЗАЦИИ СЕРВИСА СТРУКТУРИРОВАНИЯ ДАННЫХ (обновляется по мере выполнения этапов; после завершения этапа меняем статус на «выполнено»)

1. Анализ требований и подготовка окружения — статус: выполнено
   - Подробно изучить docs/requirements.md и docs/arch-rules.md, зафиксировать открытые вопросы.
   - Определить необходимые переменные окружения (.env, .env.example) и структуру config.yaml для ключей Google Sheets и LLM.
   - Решить, какие публичные Python-библиотеки понадобятся (gspread/google-api-python-client, httpx/requests, pydantic и т.п.), и согласовать с политиками безопасности.
   - Результат: набор уточнённых требований, список зависимостей и структура конфигов, описанные в docs/architecture.md.

2. Базовая структура приложения и конфигурационный слой — статус: выполнено
   - Организовать каталоги (app/core, app/adapters и пр.), добавить загрузчик конфигурации (pydantic-settings/датаклассы) с поддержкой .env/.env.example и config.yaml.
   - Реализовать логику чтения mapping-файла (JSON/YAML) и валидацию его схемы.
   - Обновить Dockerfile/requirements.txt под реальные зависимости, убедиться в совместимости с Docker Desktop.
   - Результат: контейнеризуемый каркас приложения, который стартует и валидирует конфиги без бизнес-логики.

3. Адаптер источника (Google Sheet A) — статус: выполнено
   - Имплементировать клиент Google Sheets с сервисным аккаунтом и кэшируемыми токенами (работа через Docker Desktop).
   - Добавить выборку батча строк по статусу status_new, маппинг колонок через конфиг и пагинацию.
   - Обработать пустые product_content/product_id_hash (немедленный статус «Ошибка» + note).
   - Результат: модуль, возвращающий нормализованные DTO строк-источников.

4. Клиент LLM и менеджер подсказок — статус: выполнено
   - Реализовать модуль формирования промпта (product_content + category + опциональные метаданные).
   - Подключить HTTP-клиент с ретраями, таймаутами, логированием; предусмотреть мок-режим для тестов.
   - Добавить валидацию JSON-ответа по схеме (pydantic/models), сохранить опционально llm_raw.
   - Результат: стабильный клиент, возвращающий структурированный объект или управляемую ошибку.

5. Движок сопоставления (mapping engine) — статус: выполнено
   - Реализовать функцию apply_mapping(rule, json_obj, source_row) с поддержкой source=json|source_row|const.
   - Добавить трансформации (strip, join, to_number и др.) и флаг write_if_empty.
   - Собрать патч target_column → value, пропуская пустые значения; подготовить unit-тесты на edge-cases.
   - Результат: самодостаточный слой трансформаций, покрытый тестами в tests/.

6. Адаптер приёмника (Google Sheet B) и режимы записи — статус: выполнено
   - Добавить операции append и upsert_by_xml_id (по IE_XML_ID = product_id_hash) без перезаписи неприсутствующих колонок.
   - Встроить поддержку записи только изменённых колонок; обеспечить сохранение image_path без модификаций.
   - Протоколировать успешные/неуспешные операции и возвращать результат в оркестратор.
   - Результат: модуль sink_writer, умеющий создавать/обновлять строки по патчу.

7. Оркестрация пайплайна и обработка статусов — статус: выполнено
   - Сборка основного воркфлоу: чтение батча → LLM → mapping → запись в приёмник → обновление статусов источника (Обработано/Ошибка).
   - Добавить обработку частичных ошибок, retries, подсчёт метрик, логирование и CLI-интерфейс (click/typer).
   - Предусмотреть graceful shutdown и конфигурируемые лимиты RPS/RPM.
   - Результат: исполняемый скрипт `python -m app.main` / docker run, который обрабатывает партию данных end-to-end.

8. Тестирование, QA и документация — статус: выполнено
   - Написать unit-тесты для конфигов, mapping engine, статусных переходов; добавить интеграционные тесты с моками API (pytest + responses/vcrpy).
   - Обновить README.md с инструкцией развёртывания исключительно через Docker Desktop, описать переменные окружения и сценарии запуска.
   - Подготовить чек-лист в docs/plan.md (отметки «выполнено») и убедиться, что docs/architecture.md, .env/.env.example и tests/ синхронизированы.
   - Результат: зелёные тесты в папке tests, задокументированный процесс запуска/деплоя и актуальные описания.
